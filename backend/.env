# MongoDB Configuration
MONGO_URI=mongodb://localhost:27017/addressform

# Server Configuration
PORT=5000
NODE_ENV=development

# Optional: If using MongoDB Atlas
# MONGO_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/addressform?retryWrites=true&w=majority

# Optional: JWT Secret (for future authentication)
# JWT_SECRET=your-super-secret-jwt-key-here

# Optional: CORS Origins (comma separated)
# CORS_ORIGINS=http://localhost:3000,http://localhost:3001